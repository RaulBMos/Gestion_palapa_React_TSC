# ============================================================
# Environment Variables Template
# ============================================================
# INSTRUCCIONES:
# 1. Copia este archivo a .env.local
# 2. Reemplaza los valores de ejemplo con tus credenciales reales
# 3. NUNCA subas .env.local a GitHub (ya está en .gitignore)
# ============================================================

# ------------------------------------------------------------
# Gemini AI Configuration
# ------------------------------------------------------------
# Obtén tu API key de: https://makersuite.google.com/app/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# ------------------------------------------------------------
# Supabase Configuration
# ------------------------------------------------------------
# Obtén estos valores de: https://app.supabase.com
# Ve a: Settings → API

# URL de tu proyecto Supabase
# Ejemplo: https://abcdefghijklmnop.supabase.co
VITE_SUPABASE_URL=your_supabase_url_here

# Anon/Public Key (la clave larga que empieza con "eyJ...")
# Esta clave ES SEGURA para usar en el frontend (RLS protege los datos)
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ------------------------------------------------------------
# Feature Flags
# ------------------------------------------------------------

# Habilitar/Deshabilitar Supabase
# Valores: "true" o "false"
# false = Usar localStorage (modo offline)
# true = Usar Supabase (modo cloud)
VITE_USE_SUPABASE=false

# Usar proxy API para Gemini (para producción)
# false = Llamada directa desde el navegador
# true = A través de servidor proxy (más seguro)
VITE_USE_PROXY_API=false

# ------------------------------------------------------------
# Proxy Server Configuration (Optional)
# ------------------------------------------------------------
# Si usas el servidor proxy para Gemini
# VITE_PROXY_URL=http://localhost:3001

# ------------------------------------------------------------
# Development/Production Mode
# ------------------------------------------------------------
# Vite lo maneja automáticamente, pero puedes especificarlo:
# VITE_MODE=development
# VITE_MODE=production

# ============================================================
# NOTAS DE SEGURIDAD
# ============================================================
# ✅ SEGURO compartir:
#    - VITE_SUPABASE_URL
#    - VITE_SUPABASE_ANON_KEY (protegido por RLS)
#
# ❌ NUNCA compartir:
#    - VITE_GEMINI_API_KEY
#    - Supabase SERVICE_ROLE key (no se usa en frontend)
#    - Database passwords
#
# ℹ️ La anon key es segura porque:
#    - Row Level Security (RLS) filtra los datos por usuario
#    - Solo puede acceder a datos autorizados
#    - No puede hacer operaciones administrativas
# ============================================================
